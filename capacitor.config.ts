import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.knitIt',
  appName: 'knitItMobileApp',
  webDir: 'dist/knit-it-mobile-app',
  bundledWebRuntime: false
};

export default config;
